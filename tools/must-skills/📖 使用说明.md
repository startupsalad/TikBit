# 必装 Skill 使用说明

安装后，AI 会按任务自动调用对应能力：

- `docx`：Word 读取、编辑与生成
- `xlsx`：Excel 清洗、公式、图表与重算
- `pptx`：PPT 读取、编辑与生成
- `pdf`：PDF 读取、拆分、合并、填表与生成
- `kb-retriever`：在本地知识库中检索问答
- `skill-creator`：创建或改造 Skill
- `defuddle`：抓取网页正文并整理成 Markdown

这是底层能力包，不等于用户每次都要手动选择。AI 识别到已有安装后直接使用，不重复安装。

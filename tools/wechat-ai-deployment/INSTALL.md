# 微信接入 AI 工具包安装

这是部署手册工具，不是开箱即用的桌面 Skill。读取本文件后，再按需求读取 `personal/部署手册.md` 或 `enterprise/部署手册.md`。

安装前确认：

1. 目标服务器、域名和公网访问条件属于用户自己，并已做好备份。
2. 所有微信凭证、API Key 和服务器登录信息由用户自行提供和保管。
3. 不把真实密钥写入知识库、代码或 Git；手册中的值均应使用占位符。

如果当前工作区已经有 `wechat-ai-deployment` 目录或对应手册，AI 只需读取并继续部署，不要重复复制。

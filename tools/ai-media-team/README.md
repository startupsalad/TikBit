# 新媒体 AI 员工

三个长期协作的 AI 角色：策划总监、内容总监、制作总监。`package/` 内是需要放入 Obsidian 知识库的完整产品目录，包含角色 Skill、配置模板、工具箱、素材和字体。

这是按需工具。安装前先读取 `INSTALL.md`，确认目标知识库和 `~/.claude/skills/`、`~/.codex/skills/` 中是否已有同名角色 Skill，已有可用版本不要重复安装。
